RewriteEngine On

# Si el archivo o directorio existe, servir directamente
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d

# Redirigir todo al index.php de la API
RewriteRule ^(.*)$ index.php [QSA,L]
